# Required — OpenRouter API key (https://openrouter.ai/keys)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional — defaults to 5001 if not set
PORT=5001

# Optional — logging level (trace, debug, info, warn, error)
LOG_LEVEL=info
